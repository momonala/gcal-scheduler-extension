/** Default OpenAI model when none is selected. */
export const DEFAULT_MODEL = 'gpt-5.2';

/** Default values for chrome.storage.sync.get(). */
export const STORAGE_DEFAULTS = { apiKey: '', model: DEFAULT_MODEL };
